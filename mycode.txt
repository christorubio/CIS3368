This is my code file
Python here... 

Class File
class Contacts:
    def __init__(self, id, contactDetails, creationDate):
        self.id = id
        self.contactDetails = contactDetails
        self.creationDate = creationDate
        
More python code here...
from contacts import Contacts
import datetime
from datetime import date
import mysql.connector
from mysql.connector import Error

def create_connection(host_name, user_name, user_password, db_name):
    connection = None
    try:
        connection = mysql.connector.connect(
            host=host_name,
            user=user_name,
            passwd=user_password,
            database=db_name
        )
        print("Connection to MySQL DB successful")
    except Error as e:
        print(f"The error '{e}' occurred")

    return connection

def execute_query(connection, query,):
    cursor = connection.cursor()
    try:
        cursor.execute(query)
        connection.commit()
        print("Query executed successfully")
    except Error as e:
        print(f"The error '{e}' occured")

def execute_read_query(connection, query):
    cursor = connection.cursor()
    result = None 
    try:
        cursor.execute(query)
        result = cursor.fetchall()
        return result
    except Error as e:
        print(f"The error '{e}' ocurred")

connection = create_connection("cis3368.cmzgnnpcpmtg.us-east-2.rds.amazonaws.com", "crubio", "iLOVEjesus1019!", "cis3368DB")

#Defining what happens to the database depending on the user option
#if user inputs 'a'
def addContacts():
    nombre = input("Contact name (First and Last initial): ")
    fecha = input("Todays date (YYYY-MM-DD): ")
    addInput = "INSERT INTO contacts (contactDetails, creationDate) VALUES ('%s', '%s')" % (nombre, fecha)
    execute_query(connection, addInput)

#if user inputs 'u'
def updateContacts():
    selectedID = int(input("Enter Contact ID you wish to update: "))
    updated_contact = input("Enter new contact details (First and Last initial): ")
    updateQuery = "UPDATE contacts SET contactDetails = '%s' WHERE id = %s" % (updated_contact, selectedID)
    execute_query(connection, updateQuery)

#if user inputs 'd'
def deleteContacts():
    selectedID = int(input("Enter Contact ID you wish to delete: "))
    deleteQuery = "DELETE FROM contacts WHERE id = %s" % (selectedID)
    execute_query(connection, deleteQuery)

#if user inputs 'o'
def allContacts():
    allOutput = "SELECT * FROM contacts"
    contacts = execute_read_query(connection, allOutput)
    print(contacts)

#if user inputs 'b'
def alphaContacts():
    alphaOutput = "SELECT * FROM contacts ORDER BY contactDetails"
    alphabet = execute_read_query(connection, alphaOutput)
    print(alphabet)

#if user inputs 'c'
def dateContacts():
    dateOutput = "SELECT * FROM contacts ORDER BY creationDate"
    creation = execute_read_query(connection, dateOutput)
    print(creation)

#Defining the menu of choices and getting the user input
def menu(): 
    print("MENU \n a - Add contact \n d - Remove contact \n u - Update contact details \n b - Output all contacts in alphabetical order\n c - Output all contacts by creation date \n o - Output all contacts \n q - Quit")

menu()


user_input = input("\nChoose an option: ")

#If/Else statements depending on what the user inputs
if user_input == 'o':
    allContacts()
elif user_input == 'b':
    alphaContacts()
elif user_input == 'c':
    dateContacts()
elif user_input == 'a':
    addContacts()
elif user_input == 'u':
    allContacts()
    updateContacts()
elif user_input == 'd':
    allContacts()
    deleteContacts()
elif user_input == 'q':
    quit
else:
    print("Please insert menu option.")




More code here
